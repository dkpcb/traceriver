# repository/

Persistence abstractions (interfaces only).

**Responsibilities:**
- Define persistence interfaces
- No implementations here (implementations go in infrastructure/)

**Dependencies:**
- Can depend on: domain only
- Must NOT depend on: any other layer
